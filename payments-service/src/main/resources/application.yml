spring:
  application:
    name: payments-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      label: 005-Containers # default
  profiles:
    active: development
  redis:
    host: redis-service