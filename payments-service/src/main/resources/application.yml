spring:
  application:
    name: payments-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      label: 003-communication
  profiles:
    active: development